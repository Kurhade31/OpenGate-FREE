{
  "name": "gate-free-platform",
  "version": "0.1.0",
  "description": "Free, open-source, student-first GATE 2027 preparation platform (static-first, no-backend MVP).",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "typecheck": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test": "vitest run",
    "validate:data": "tsx scripts/validate-content.ts",
    "validate:sources": "tsx scripts/validate-sources.ts",
    "check:links": "tsx scripts/check-links.ts"
  },
  "dependencies": {
    "astro": "^5.13.2",
    "@astrojs/sitemap": "^3.2.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.8",
    "tsx": "^4.19.2"
  }
}
